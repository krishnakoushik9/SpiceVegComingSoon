{
  "name": "spice-veg-agri",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "turbo build",
    "deploy:web": "cd apps/web && npx wrangler pages deploy out --project-name=spiceveg-web",
    "deploy:admin": "cd apps/admin && npx wrangler pages deploy out --project-name=spiceveg-admin",
    "deploy:verify": "node scripts/stage-verify.mjs && npx wrangler pages deploy dist/verify --project-name=spiceveg-verify --branch=main --commit-dirty=true",
    "deploy:api": "cd apps/worker && npx wrangler deploy",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.2"
  },
  "dependencies": {
    "hono": "^4.4.2",
    "zod": "^3.23.8"
  }
}
